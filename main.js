import {
  addToCart,
  decrementItem,
  addItem,
  selectedImage,
  openCardCart,
  removeCardCart,
  openImage,
  closeImage,
} from "./src/js/selectors";

addItem();
decrementItem();

addToCart();
openCardCart();
removeCardCart();

selectedImage();
openImage();
closeImage();
