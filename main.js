import {
  addToCart,
  decrementItem,
  addItem,
  selectedImage,
  openCardCart,
  removeCardCart,
} from "./src/js/selectors";

addItem();
decrementItem();

addToCart();
openCardCart();
removeCardCart();

selectedImage();
