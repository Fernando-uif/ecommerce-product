import {
  addToCart,
  decrementItem,
  addItem,
  selectedImage,
  openCardCart,
  removeCardCart,
  openImage,
} from "./src/js/selectors";

addItem();
decrementItem();

addToCart();
openCardCart();
removeCardCart();

selectedImage();
openImage();
